import React from 'react';
import {
  Animated,
  View,
  Text,
  SafeAreaView,
  TouchableOpacity,
} from 'react-native';

export const ScreenVerification = ({ route, navigation }) => {
    return(
        <Text>Verification</Text>
    )
}